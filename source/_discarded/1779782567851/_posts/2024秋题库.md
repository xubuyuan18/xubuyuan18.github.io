---
title: 2024秋题库
date: 2024-12-12 08:48:34
tags:
  - 题库
---

## 工业互联网络技术

#### 什么是ACL

> 


ACl是在交换机和路由器上经常采用的一种流量交换技术，它通过制定一定的规则对经过的网络设备的数据包经行过滤，以实现允许或者拒绝特定的数据包进出网络，实现对网络的访问进行控制




#### H3C具有哪些视图

> 


用户视图 


配置视图 


接口视图 


路由协议视图 


用户界面视图




#### 什么是vlan

> 


vlan是虚拟局域网的简称，它利用交换机在一个物理网络上划分多个逻辑网络的技术。根据功能，部门划分多个多个逻辑工作组，形成不同的虚拟网络。




#### 简述路由器的路由表查找规则

> 


最长匹配原则;当数据包到达路由时，路由会根据数据包的目的地址与路由表进行匹配，最后选择一条匹配最长的路由进行数据转发
下一条地址原则：当路由 查找到匹配的目的地址的路由路由条目时，判断下一跳地址是否直连，如果直连则直接转发，则以下一跳地址为目的地址继续匹配路由，直到匹配路由，然后转发
默认路由原则：当路由器收到一个数据包，当目的地址与自己所有路由条目都没有匹配上，此时如果存在默认路由，则通过默认路由转发，如果不存在默认路由，则直接丢弃该数据包




#### 简述IP地址动态获取过程

> 


DHCP客户端请求ip dhcp客户端以广播的方式发出DHCP discover报文


DHCP 服务器响应 DHCP服务器向DHCP客户端发送一个DHCP offer报文


DHCP客户端选择IP  DHCP客户端会发出一个广播DHCP request报文，在选项字段中会加入选中的DHCP服务器的IP地址和需要的IP地址


DHCP服务端确认租约 DHCP服务器向DHCP客户端响应一个DHCP ACK报文，并在选项字段中增加IP地址的使用租期信息




#### 简述RIP的缺陷

> 


以跳数评估路由并非最佳路径
rip路由协议会引发环路
更新发送全部路由表 浪费网路资源
收集信息慢
发布信息以及认证方式-协议的安全性
最大跳数16导致网络尺度小




#### 什么是链路聚合

> 


链路聚合是将多个物理以太网口聚合在一起形成一个逻辑上的聚合组，使用链路聚合服务的上层实体把同一聚合组的多条物理链路视为一条逻辑链路




#### 简述STP工作过程

> 


选举一个根网桥
在每一个非根网桥上选举一个根端口
在每一个网段上 选举一个指定端口
阻塞非根，非指定端口




#### 简述STP作用

> 


通过阻断冗余链路来消除桥接网络中可能存在的路径回环
当路径发生故障时，激活冗余备份链路，恢复网络连通性




#### 简述路由器的作用

> 


连接具有不同介质的链路


连接网络或子网，隔离广播


对数据报文执行寻路和转发


交换和维护路由信息




#### 简述NAT分类特点

> 


basic nat


私网与公网IP一对一映射，仅能实现内网与公网通信不能实现节约IP地址的作用，现实中基本不会使用


NAPT


私网IP+端口号与公网IP+端口号多对一映射，不仅实现了内网与公网通信，并且节约了IP地址。在现实中广泛使用于企业校园网


Easy IP


私网IP+端口号与出口IP+端口号的多对一映射 。特点同上，广泛用于家庭网络。




#### 简述动态路由协议的工作原理

> 


网络中所有路由都开启相同的路由协议


邻居发现：路由器通过发送广播报文或发送给指定的路由器邻居以主动地把自己介绍给网段内的其他路由器


路由交换：每台路由器将自己路由相关的信息发送给相邻路由器


路由计算；每台路由运行某种算法，计算出最终的路由来


路由维护：路由器之间通过周期性的发送 协议报文来维护邻居信息




#### 简述静态路由的优缺点

> 


优点


网络管理员3手工配置，无开销


网络安全保密性好


缺点


网络拓扑变化时需管理员重新手工配置路由


网络规模过大时管理员工作量大




#### 简述DHCP的特点

> 


即插即用性：客户端无需配置即能获得IP地址及其参数。建华客户端网络配置，降低维护成本


统一管理：所有IP地址及其相关参数信息由DHCP服务器统一管理，统一分配


可跨网段实现：通过使用DHCP中继，可使处于不同子网中的客户端和DHCP服务器之间实现协议报文交互




#### 在显示网络中，DHCP服务器需要为DHCP客户端分配哪些参数？

> 


IP地址


子网掩码


默认网关


DNS服务器地址




#### 衡量路由协议的主要指标

> 


协议计算的正确性


协议使用算法能否正确算出最优路由，且正确无环


路由收敛速度


当网络拓扑发生变化时，能迅速感知并及时更新响应的路由信息


协议占用系统开销


协议自身的开销小


协议自身安全性


协议自身不易受攻击，有安全机制


协议适用网络规模


协议可以应用在任何拓扑结构和规模的网络中




#### 在运行了RIP的MSR路由器上看到如下信息

> 


目的网络 ：6.6.6.0/24


路由协议：RIP


路由优先级：100


路由花销值：1


下一跳地址：100.1.1.1


出接口：g0/0




## Open Euler

#### 假设一个设备的文件名为dev/sdb，可以确认它是主板上第二个插槽上的设备吗？

> 


不一定，因为设备文件名是由系统识别顺序确定的




#### Linux操作系统中的DNS服务器主要有哪几种类型？

> 


缓冲域名服务器 


主域名服务器 


从域名服务器




#### RAID技术主要解决什么问题？

> 


RAID技术可以解决储存设备的读写问题及数据的冗余备份问题




#### 若想查看的文件具有较长的内容，那么使用cat，more，head，tail中那个问题命令最合适

> 


more




#### 简述Linux系统分区原理，分区，设备名称，格式化以及挂载

> 


分区：将一大块硬盘分为若干小的逻辑分区
格式化：写入文件系统
分区设备文件名；Linux系统中，每个分区都会定义设备文件名，依据设备文件名找分区
挂载：给每个分区分配挂载点，也就是建立分区与目录的连结，如
/ 根
/swap 交换
/boot 启动




#### 测试DNS服务器和配置是否正确有哪些方法

> 


nslookup
ping




#### 对逻辑卷扩容，缩容有何异同点

> 


同：都需要先取消逻辑卷与目录的挂载关联
异：扩容先扩容后检查文件系统完整性
	缩容为保证数据安全，先检查文件系统完整性在缩容




#### 输出重定向>与>>的区别是什么？

> 


‘>’是定向输出文件，如果文件存在就清空，如果不存在就创建


‘>>’是将输出内容追加到目标文件中，如果文件存在就将新的内容追加到文件末尾，如果不存在就创建




#### 列举Linux操作系统的主要特点

> 


开放性 


多用户 


多任务 


设备独立性 


丰富的网络功能 


可靠的系统安全 


良好的可移植性




#### RAID 0和RAID 5哪个更安全？

> 


RAID 0 没有数据冗余功能，所以RAID 5 更安全。




#### 有哪些命令可以用来查看文件的内容，这些命令有什么特点？

> 


cat 合并和显示文件内容适合查看小文件，简单易用
less 分页显示文件内容，允许前后翻页，适合查看大文件
more 分页显示文件内容，只能向前翻页
head 显示文件的开头部分默认显示10行
tail 显示文件的末尾部分，默认显示后10行




#### 如果硬盘需要5个分区，则至少需要几个逻辑分区？

> 


2个逻辑分区




## 传感网

#### 请说明说stm32f10vet6型号中各部分的意义

> 


stm32 st公司出品的基于arm内核的32位微控制器
f	基础型
103	stm32基础型
v	引脚数为100
E	内存容量为512kb
T	封装方式为QFP封装
6	工作温度范围为-40到+85




#### ADC的基本参数

> 


ADC的工作模式
ADC转换结果的储存方式
ADC通道的转换时间
ADC时钟信号的获取




#### 实现呼吸灯的原理是什么

> 


PWM采用调整脉冲占空比的方式，达到调整电压与电流的效果




#### 串口中断模式发送和接收数据

> 


HAL_UART_Transmit_IT(); 串口中断模式发送
HAL_UART_Receive_IT();	串口中断模式接收




#### 串口发送和接收函数

> 


HAL_UART_Transmit();串口轮询机制发送，使用超时管理机制
HAL_UART_Receive();串口轮询机制接收，使用超时管理机制




#### can通信帧的类型

> 


数据帧 遥控帧 错误帧 过载帧 帧间隔




## openstack 云平台

#### 创建安全组虚拟机

```bash
open stack server create --flavor flavor-02 --image ubuntu-img-01 --nic net-id=net-03b --security-group web-sg webserver-vm

```


#### 删除网络

```bash
先删除子网:open stack subnet delete unused-subnet

再删除网络:openstack network delete unused-network

```


#### 查看虚拟机情况

```bash
openstack server show qppserver-vm -c address -c os-extened-volunmes:volumes_attached -c networks  -f value | grep -E "address|mac"

```


#### 创建主机

```bash
openstack server creat VM_host --image cirros --flavor myflavor --network vm-network

```


#### 挂起恢复

```bash
挂起命令:openstcak server suspend dbserver-vm;
恢复命令:openstack server resume dbserver-vm

```


#### 加密密钥

```bash
openstack volume create --size 50 --volume-type high-perf --encryption-provider luks --encryption-key-id encrypt-key-01 highperf-volume

```


```bash
创建网络 openstack network create test-isolate-network
创建子网 openstack subnet create --network tes-isolate-network --subnet-range 172.16.10.0/24 --gateway 172.16.10.1 --dns-namerver 8.8.8.8 test-subnet

```


```bash
创建网络
openstack subent create --network vm-network --allocation pool start=192.168.20.100.end=192.168.20.200 --dns-nameserver 114.114.114.114 --subnet-range 192.168.20.0/24 vm-subnetwork

```


#### 上传镜像

```bash
open stack image create --file win-server-custom.qcow2 --disk-format qcw2 --container-format bare --property tags="windows,server" win-server-optimized

```